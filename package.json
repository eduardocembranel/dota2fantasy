{
  "name": "dota2fantasy",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "generate:match-metrics-index": "node scripts/generate-match-metrics-index.mjs",
    "dev": "npm run generate:match-metrics-index && vite",
    "build": "npm run generate:match-metrics-index && tsc && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "typescript": "~5.7.2",
    "vite": "^6.0.11"
  }
}
